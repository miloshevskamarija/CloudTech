data_dir: "data/raw"
file_pattern: "*.4lep.root"

dask:
  n_workers: 4
  threads_per_worker: 1

selection:
  pt_min: 5.0
  eta_max: 2.5

hist:
  nbins: 110
  min: 50    # GeV
  max: 800   # GeV

output_dir: "outputs"

parallel: true
n_workers: 4
