data_dir: "data/raw"
file_pattern: "*.4lep.root"

selection:
  pt_min: 5.0
  eta_max: 2.5

hist:
  nbins: 110
  min: 50    # GeV
  max: 800   # GeV

output_dir: "outputs"

# Default number of worker processes for run_analysis.py
n_workers: 4

analysis:
  compute_extra_observables: false   # pure timing tests
  make_plots: false                  # avoid matplotlib overhead
